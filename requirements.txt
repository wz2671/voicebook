loguru>=0.7.0
standard-imghdr>=1.0.0; python_version >= "3.11"

# Edge-TTS dependencies (for src/libs/edge_tts)
aiohttp>=3.8.0
certifi>=2023.0.0
typing_extensions>=4.0.0
tabulate>=0.9.0

# ChatTTS dependencies (for src/libs/chat_tts)
torch>=2.0.0
torchaudio>=2.0.0
# 注意：ChatTTS 首次使用会自动下载模型到 ~/.cache/huggingface/
# GPU模式需要 CUDA 支持，CPU模式可直接运行
